package day12_GreedyMethods;

import java.io.*;
import java.util.StringTokenizer;

public class Q4_FuelEconomy {
    public static void main(String[] args) throws IOException {
        BufferedReader in = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(in.readLine());
        int stationCount = Integer.parseInt(st.nextToken());
        int tankCapacity = Integer.parseInt(st.nextToken());
        int startFuel = Integer.parseInt(st.nextToken());
        int totalDist = Integer.parseInt(st.nextToken());

    }
}
